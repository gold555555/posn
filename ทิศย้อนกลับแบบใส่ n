#include <bits/stdc++.h>
using namespace std;


int main() {
	int n ;
	cin >> n;
	char arr[n];
	for(int i=0 ; i<n ; i++){
		cin >> arr[i];
	}
		if(arr[0] == 'N' || arr[0] == 'S'){
			reverse(arr,arr+n);
		
		}else{
			return 0 ;
		}
		cout << arr << endl ;
}
